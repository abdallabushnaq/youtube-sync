<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (C) 2025-2026 Abdalla Bushnaq
  Licensed under the Apache License, Version 2.0 (the "License").

  Test-only Logback configuration.
  Logs are written to target/test.log so they do not interfere with
  the progress bars rendered on the terminal during test runs.
-->
<configuration>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/test.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} — %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress noisy Google HTTP client debug output unless explicitly needed -->
    <logger name="com.google" level="WARN"/>
    <logger name="org.eclipse.jetty" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="FILE"/>
    </root>

</configuration>

